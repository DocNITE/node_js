{
  "name": "entry-point",
  "version": "1.0.0",
  "description": "server-client",
  "main": "index.js",
  "scripts": {
    "build-client": "cd client && npm run build",
    "build-server": "cd server && npm run build",
    "build": "npm run build-client && npm run build-server",
    "start": "cd server && npm run start",
    "start-build": "npm run build && npm run start",
    "start-build-client": "npm run build-client && npm run start"
  },
  "author": "DocNITE",
  "license": "ISC",
  "dependencies": {
    "rimraf": "^5.0.0",
    "ts-loader": "^9.4.2",
    "typescript": "^5.0.4",
    "webpack": "^5.82.0",
    "webpack-cli": "^5.0.2"
  }
}
